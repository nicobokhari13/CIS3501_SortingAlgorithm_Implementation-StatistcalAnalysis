# Experiment Execution Commands

In the project root, run `cmake .`, then `make`

The executable defined in `apps/CMakeLists.txt` is outputted into the `apps` directory.

Run the executable with `./apps/<executable-name>`